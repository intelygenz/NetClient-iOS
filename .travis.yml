language: swift
osx_image: xcode9.4
script:
- xcodebuild -workspace Net.xcworkspace -scheme Example -destination "platform=iOS
  Simulator,name=iPhone X,OS=11.4" -configuration Debug -enableCodeCoverage YES clean
  build test
after_success:
- bash <(curl -s https://codecov.io/bash)
- sh Scripts/gnscharts.sh Net Example IG27 none Testing
